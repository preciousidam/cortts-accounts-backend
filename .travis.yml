language: python

python:
  - 3.6
  - 3.7
  - 3.8

install: 
  - pip install -r requirements.txt

script:
  - python -m pytest -v

deploy:
  provider: heroku
  api_key:
    secure: 
  app: cortts-accounts-backend
  on:
    repo: preciousidam/cortts-accounts-backend